version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - govet
    - misspell
    - noctx
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports
